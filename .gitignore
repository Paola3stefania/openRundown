node_modules/
dist/
.env
*.log
*.pem
*.key

# Team member data
members.csv

# IDE files
.idea/
.cursorrules

# Cache and data files - ignore all JSON files in these directories
cache/**/*.json
results/**/*.json

# Keep cursor config example but ignore personal config
cursor-mcp-config.json
.vercel

# Machine-specific MCP launcher (contains absolute paths)
run-mcp.sh

# Local-only scripts, notes, and one-off SQL (never commit)
local/
